# Brainstorm: Novel Optimization Approaches (Day 7)

- LLM-guided evolutionary operators (mutation = "suggest a new architecture variant")
- Physics-informed neural network (PINN) as fast surrogate inside the optimizer
- Multi-fidelity Bayesian optimization with low-fidelity first-order model + high-fidelity corrections
- Human-in-the-loop interactive optimization where designer steers Pareto selection via natural language

**Idea**: Implement a very lightweight version of LLM + GA hybrid even if only for 2-3 generations.
